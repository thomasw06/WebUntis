# Minimal placeholder for GitHub Pages / Jekyll so the docs folder is always valid.

---

## title: WebUntis Sync

This repository generates a WebUntis iCalendar file (calendar.ics) and publishes it via GitHub Pages.

- The generated file is: output/calendar.ics
- After forking, follow the instructions in README.md to configure your secrets, enable Actions, and enable GitHub Pages.